<?php
  //Get name of the uploaded file
  $filename = $_FILES["file"]["name"]
  
  //Choose where to uploaded a file
  $location = "upload/".$filename
  
  //Save file to local file system
  if(move_uplaoded_file($_FILES["file"]["tmp_name"], $location)
  {
    echo "Success"
  }
  else
  {
    echo "Failure"
  }

?>